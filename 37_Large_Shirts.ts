
function make_shirt(size= "L", message = "I Love Type Script"){
    console.log("The size of the Shirt is " + size +" & " + message);
};

// Deffault message with size L
make_shirt()

// Default message with size M
make_shirt("M")

// Default message with selected size
make_shirt("S", "Fun with Python")